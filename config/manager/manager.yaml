apiVersion: v1
kind: Namespace
metadata:
  labels:
    control-plane: nervex-operator
  name: nervex-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nervex-operator
  namespace: nervex-system
  labels:
    control-plane: nervex-operator
spec:
  selector:
    matchLabels:
      control-plane: nervex-operator
  replicas: 1
  template:
    metadata:
      labels:
        control-plane: nervex-operator
    spec:
      containers:
      - command:
        - /manager
        args:
        - --leader-elect
        image: registry.sensetime.com/cloudnative4ai/nervex-operator:v0.0.1-alpha.0-f819fc68
        imagePullPolicy: Always
        name: manager
        securityContext:
          allowPrivilegeEscalation: false
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 100m
            memory: 30Mi
          requests:
            cpu: 100m
            memory: 20Mi
      terminationGracePeriodSeconds: 10
